{
  "name": "volcano-gen2",
  "version": "1.1.1",
  "private": true,
  "license": "MPL-2.0",
  "scripts": {
    "bootstrap": "bash ./install_dep.sh",
    "check": "make check",
    "fix": "make format",
    "verify": "npm run bootstrap && npm run check",
    "repair": "npm run fix && npm run check"
  },
  "dependencies": {
    "scoop": "^0.0.2"
  }
}
